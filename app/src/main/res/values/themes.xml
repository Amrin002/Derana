<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Derana" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily">@font/jakarta_sans_reguler</item>
        <item name="android:statusBarColor">@color/primaryColor</item>
        <item name="statusBarBackground">@color/primaryColor</item>
        <item name="background">@color/white</item>



    </style>

    <style name="Theme.Derana" parent="Base.Theme.Derana" />
</resources>